# 租赁合同管理系统 Web 版依赖
# 安装：pip install -r requirements.txt
# 兼容 Python 3.10+（本机系统 Python 3.11 实测）
#
# 安全下限说明：
#   reportlab 必须 >= 3.6.13 —— 3.6.12 及以下存在 CVE-2023-33733
#   （Paragraph 的 rl_safe_eval 代码注入，可导致服务器 RCE）。
#   本系统会把数据库中的用户数据（承租方/备注/Excel 导入单元格）送入 Paragraph，
#   属真实可利用路径。代码侧已做 XML 转义（excel_utils._pdf_text）作为双保险，
#   但依赖版本本身亦不得低于安全线。回归见 backend/test_pdf_security.py。

fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.9
openpyxl>=3.0.9
reportlab>=3.6.13
PyJWT>=2.8.0

# 可选增强（R-05，未装也不影响运行）：psutil 用于数据库锁定时的进程检测（db_utils 可选调用）
# psutil>=5.9
